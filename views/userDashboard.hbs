<section class="bg">
    <div class="container">
        <h2 class="text-center text-dark fs-2 fw-bold">Welcome <span>{{student.name}}</span>!</h2>
        {{!-- <p class="text-center text-warning">Update profile.</p> --}}
        <div class="row text-center d-flex">
            <div class="col-lg-12">
                {{#if status}}
                <a href="/register-exam" class="btn " style="background-color: lightblue;">Register Exam</a>
                {{else}}
                <a class="btn " href="/student-register" style="background-color: lightblue;">Register Details</a>
                {{/if}}
            </div>
        </div>
    </div>
</section>
<style>
    .divider:after,
    .divider:before {
        content: "";
        flex: 1;
        height: 1px;
        background: #eee;
    }

    .h-custom {
        height: calc(100% - 73px);
    }

    @media (max-width: 450px) {
        .h-custom {
            height: 100%;
        }
    }

    .bg {
        background: url('/img/user_dash.png') no-repeat center;
        background-size: auto;
        height: 90vh;
    }
</style>