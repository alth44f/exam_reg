<section class="bg">
    <div class="container">
        <h2 class="text-center text-light fs-2 fw-bold">Welcome <span style="color: #5fcf80;">{{student.name}}</span>!</h2>
        <div class="row text-center d-flex">
            <div class="col-lg-12">
                {{#if status}}
                <p class="text-light">Welcome back! Please complete the necessary actions to proceed.</p>
                <a href="/register-exam" class="btn btn-success text-light fw-bold">Exam Registration</a>
                {{else}}
                <p class="text-light">It seems you're not yet registered. Please complete your registration process.</p>
                <a class="btn btn-success text-light fw-bold" href="/student-register">Get Registered</a>
                {{/if}}
            </div>
        </div>
    </div>
</section>
<style>
    .divider:after,
    .divider:before {
        content: "";
        flex: 1;
        height: 1px;
        background: #eee;
    }

    .h-custom {
        height: calc(100% - 73px);
    }

    @media (max-width: 450px) {
        .h-custom {
            height: 100%;
        }
    }

    .bg {
        background: url('/img/bg.avif') no-repeat center;
        background-size: cover;
        height: 90vh;
        background-color: #606e67;
        background-blend-mode: darken;
    }
</style>